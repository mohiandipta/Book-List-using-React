import React from 'react';
import { name } from '../../data';

const Banner = () => {
    return (
        <div>
            <h1>Hello My Friend {name}</h1>
        </div>
    );
}

export default Banner;
